import React from 'react'

const ProductHeader = () => {
  return (
    <h3 className="flex justify-center items-center text-black font-spline tracking-[.01em] font-semibold text-[clamp(25px,6.944vw,50px)] pad:text-[clamp(50px,4.63vw,86px)] desktop:text-[clamp(60px,3.125vw,100vw)]">
      Product
    </h3>
  );
}

export default ProductHeader